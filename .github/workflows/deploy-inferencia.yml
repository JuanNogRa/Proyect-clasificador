# Desactivado: usar main_rag-siniestros-api.yml (OIDC desde Azure Portal)
# o volver a activar si usas Publish Profile en lugar de OIDC.
name: Deploy API Inferencia (publish profile)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow desactivado"
